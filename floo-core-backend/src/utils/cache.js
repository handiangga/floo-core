const clearAllCache = async () => {
  console.log("cache cleared (dummy)");
};

const clearCacheByPrefix = async () => {
  console.log("cache cleared (prefix)");
};

module.exports = {
  clearAllCache,
  clearCacheByPrefix,
};
